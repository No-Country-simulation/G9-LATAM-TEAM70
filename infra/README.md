# Infraestructura

La guía de despliegue para OCI está en [OCI_DEPLOYMENT.md](OCI_DEPLOYMENT.md).

Flyway se habilita únicamente con el perfil `docker` y crea el esquema MySQL desde `backend/src/main/resources/db/migration`.
